const emailPasswordInvalid = "Invalid email or password"
const missingFields = "Fields are missing"
const NotAuthorized = "You are not authorized"

module.exports = {
    emailPasswordInvalid,
    missingFields,
    NotAuthorized
}